# Supabase Realtime (browser-safe — these get bundled into the build).
# Dashboard → Project Settings → API → Project URL and the publishable key.
# Leave unset to use API polling instead of Realtime.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
