# Production build only (npm run build / Docker / deploy). Local `npm run dev`
# ignores this file and uses API polling, since the local backend writes to
# SQLite, not Supabase. The publishable key is browser-safe by design — it
# ships in the bundle and is protected by row-level security.
VITE_SUPABASE_URL=https://exvbrbcflnysytlfyuiu.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_GZVPqwePfQs_NJX6aJsaAg_ox_ootad
